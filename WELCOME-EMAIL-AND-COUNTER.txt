WELCOME EMAIL + SUBSCRIBER COUNTER — quick fix guide
====================================================

PROBLEM
-------
Some visitors do not get a welcome email.
The subscriber counter does not always update for everyone.

WHY
---
1. Render (the-blue-wave.onrender.com) has NO mail configured yet.
   Check: https://the-blue-wave.onrender.com/api/mail-status
   Should show mailConfigured: true

2. Welcome emails rely on EmailJS from the browser.
   Some phones block this (privacy settings, ad blockers, strict networks).

WHAT WE FIXED IN CODE (upload new zip to Bluehost)
--------------------------------------------------
- EmailJS is tried FIRST (with one retry) — not Render first
- Global subscriber counter uses countapi.mileshilliard.com
  (same number for all visitors worldwide)
- Counter refreshes every 15 seconds

UPLOAD
------
Double-click: bluewave\UPLOAD-THEBLUEWAVEFANS.bat
Then upload thebluewavefans-upload.zip to Bluehost (extract in public_html)

BEST FIX FOR WELCOME EMAIL (Render dashboard — 5 minutes)
-----------------------------------------------------------
Double-click: bluewave\SETUP-RENDER-MAIL-NOW.bat

IMPORTANT: Your Gmail App Password may have EXPIRED.
Create a NEW one: https://myaccount.google.com/apppasswords
(Paste without spaces into SMTP_PASS on Render.)

Add on Render (service: the-blue-wave):

   SMTP_HOST = smtp.gmail.com
   SMTP_PORT = 587
   SMTP_USER = madmax.zebra@gmail.com
   SMTP_PASS = (NEW 16-char app password)
   ADMIN_EMAIL = madmax.zebra@gmail.com
   SITE_URL = https://www.thebluewavefans.com
   RESEND_API_KEY = (optional backup — copy from backend\.env)

Save → Render redeploys (~5 min)

Check: double-click CHECK-MAIL-WORKS.bat
Should show MAIL: WORKING

After this, welcome emails go via Gmail first; EmailJS is backup only.

OPTIONAL: Web3Forms Pro autoresponder
-------------------------------------
If you have Web3Forms Pro, enable Autoresponder in:
https://app.web3forms.com
→ Form settings → Autoresponder ON
→ Subject: "You're on the list — The Blue Wave"
→ Logo: https://www.thebluewavefans.com/logo.png

This sends a second welcome path automatically.

TEST
----
1. Use a NEW email you never tested before
2. Submit on phone AND computer
3. Check inbox + spam folder
4. Counter should match on both devices within ~15 seconds
